#include <stdio.h>
int main()
{
   int array[100], c, n,sum;
   printf("Enter number of elements in array\n");
   scanf("%d", &n);
   printf("Enter %d elements\n", n);
   for (c = 0; c < n; c++)
   scanf("%d", &array[c]);
   printf("The array elements are : \n");
   for (c = 0; c < n; c++)
   printf("%d\t", array[c]);
   for (array[0]=1; array[0] < n; array[0]++)
   sum=array[%d+%d];
   printf("\ns = %d",sum); 
}

